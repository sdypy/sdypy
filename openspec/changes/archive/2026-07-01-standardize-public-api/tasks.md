## 1. SEP 2 amendment

- [x] 1.1 Open `docs/seps/sep-0002.rst` and add a new section after the existing variable table covering module/package naming (snake_case words; established acronym subpackages `EMA`, `FRF` stay uppercase as fixed by SEP 3/4), class naming (CapWords; acronyms uppercase inside CapWords, e.g. `FRF` not `Frf`), function/method/parameter naming (snake_case; acronyms lowercase inside snake_case, e.g. `add_frf`, `frf_type`; exception: established criterion functions may be the bare uppercase acronym `MAC`, `MSF`, `MCF`), constants (ALL_CAPS), framework exemption (names mandated by an external framework keep that framework's casing, e.g. Qt's `closeEvent`), public-surface rule (every first-level package declares `__all__`; only curated names are supported API), and the deprecation policy (old name kept as alias emitting `DeprecationWarning` for all of v1.x; removal no earlier than v2.0; new code uses canonical names only). Extend the canonical variable table with four evidenced entries: `frf_form` (FRF form: receptance / mobility / accelerance — canonical at the sdypy level; pyFRF's `form=` is backend scope and untouched), `young_modulus`, `poisson_ratio`, `density` (FEM material parameters, fixed compound terms). Do NOT change `:Status: Draft` — the flip to `Accepted` is a gated team act (task 10.2).
- [x] 1.2 Verify that the original content of `sep-0002.rst` is intact after amendment: PEP 8 reference, word-order rule, and the canonical variable table (`frf`, `freq`, `freq_rad`, `natural_freq`, `fs`, `dt`, `freq_upper`/`freq_lower`) must all still be present.

## 2. Core repo: umbrella `__all__`, conformance tests, and checker

- [x] 2.1 Add `__all__ = ["EMA", "io", "FRF", "excitation", "model", "view"]` to `sdypy/__init__.py`. Verify that the lazy `__getattr__` and `__dir__` implementations remain consistent with this list (all six names must appear in `__dir__` output; no name removed from `__getattr__`).
- [x] 2.2 Create `tests/test_public_api.py`. The test module MUST: (a) for each of the six first-level packages, assert `__all__` exists and is non-empty; (b) assert every entry in each `__all__` resolves via `getattr` without `AttributeError`; (c) assert none of the banned leak names (`np`, `scipy`, `warnings`, `os`, `io`, `platform`, `subprocess`, `pickle`, `tqdm`, `pv`, `CubicSpline`, `beta`, `moment`, `signal`, `signals`, `BackgroundPlotter`, `BasePlotter`, `haspyqt`, `Image`, `pyperclip`) appears in the `__all__` of any of the six first-level packages (the umbrella's `__all__` legitimately contains `io`, the subpackage name — do not apply the banned list to the umbrella); (d) assert module-type entries appear only in the sanctioned positions (io: `uff`, `lvm`, `mraw`, `sfmov`; EMA: `stabilization`, `normal_modes`, `pole_picking`; model: `lumped`, `mesh`); (e) assert the umbrella's `__all__` equals exactly the six first-level names; (f) include an advisory drift-check test for the two shims that calls `check_public_api.py` logic and asserts it exits 0 regardless of drift found. Add a comment block at the top noting these tests are expected-red against current PyPI artifacts until the batched releases ship.
- [x] 2.3 Create `tools/check_public_api.py`. The script MUST: accept `--path <sibling-clone-dir>` and audit `sdypy/<pkg>/__init__.py` for: presence of `__all__`; absence of any `import *` line; curated names matching the design-specified lists from this spec; exit non-zero and print all violations on failure. Additionally, when run against a shim clone (FRF or excitation), perform the advisory drift check: enumerate backend public callables (names not prefixed with `_`), report any not in `__all__`, and exit 0 regardless. Keep this script separate from `tools/check_sibling_template.py`.

## 3. sdypy-EMA

- [x] 3.1 Rewrite `sdypy/EMA/__init__.py` in the `packages/sdypy-EMA` clone: replace `from .tools import *` with explicit named imports of every tool module symbol needed. Add `__all__ = ["Model", "MAC", "MSF", "MCF", "complex_freq_to_freq_and_damp", "stabilization", "normal_modes", "pole_picking"]`. Ensure `np`, `tqdm`, and `warnings` are no longer reachable via `sdypy.EMA.<name>` star-import.
- [x] 3.2 Add `auto_mac` as the canonical method name on `EMA.Model`: rename the implementation of `autoMAC` to `auto_mac`, and keep `autoMAC` as a deprecated wrapper that calls `auto_mac` and emits `DeprecationWarning("autoMAC is deprecated; use auto_mac instead")`.
- [x] 3.3 Add a dual-accept parameter shim on `EMA.Model.__init__`: canonical `frf_form=` replaces `frf_type=`; calling with `frf_type=` MUST emit `DeprecationWarning` and forward to `frf_form`; positional callers unaffected.
- [x] 3.4 Add unit tests in `packages/sdypy-EMA/tests/` covering: (a) `auto_mac` callable without warning; (b) `autoMAC` callable and emits `DeprecationWarning` with the same result; (c) `Model(frf_form=...)` constructs without warning and `Model(frf_type=...)` emits `DeprecationWarning` with identical behavior; (d) `np`, `tqdm`, `warnings` not in `sdypy.EMA.__all__`; (e) every entry in `sdypy.EMA.__all__` resolves via `getattr`.
- [x] 3.5 Bump version to `0.30.0` in `pyproject.toml` (minor bump; or fold into the still-unreleased 0.29.2 from change #3 if that has not shipped to PyPI, superseding it with a single `0.30.0` release).
- [x] 3.6 Gate: run `python tools/check_public_api.py --path ../packages/sdypy-EMA` from the core repo and confirm exit 0; run `python -m build` from `packages/sdypy-EMA`; install the built wheel in a fresh venv at a neutral directory path (NOT under `C:\Users\jasas\Work\OpenSource\SdyPy\`) and confirm `python -c "import sdypy.EMA; print(sdypy.EMA.__all__)"` prints the expected list.

## 4. sdypy-io

- [x] 4.1 Rewrite `sdypy/io/__init__.py` in the `packages/sdypy-io` clone: ensure the four module-alias imports are explicit (`import pyuff as uff`, `import lvm_read as lvm`, `import pyMRAW as mraw`, `from .sfmov import sfmov`). Add `__all__ = ["uff", "lvm", "mraw", "sfmov"]`. Confirm no stdlib/third-party name is reachable via star-import.
- [x] 4.2 Bump version to `0.4.0` in `pyproject.toml` (or fold into unreleased `0.3.2` if not yet on PyPI, superseding with `0.4.0`).
- [x] 4.3 Gate: run `python tools/check_public_api.py --path ../packages/sdypy-io` from the core repo and confirm exit 0; run `python -m build` from `packages/sdypy-io`; fresh-venv install from a neutral directory path and confirm `python -c "import sdypy.io; print(sdypy.io.__all__)"` prints `['uff', 'lvm', 'mraw', 'sfmov']`.

## 5. sdypy-FRF

- [x] 5.1 Rewrite `sdypy/FRF/__init__.py` in the `packages/sdypy-FRF` clone: replace `from pyFRF import *` with `from pyFRF import FRF, assert_sep005, direction_dict`. Add `__all__ = ["FRF", "assert_sep005", "direction_dict"]`. Confirm no `import *` line remains.
- [x] 5.2 Bump version to `0.2.0` in `pyproject.toml` (or fold into unreleased `0.1.1` if not yet on PyPI, superseding with `0.2.0`).
- [x] 5.3 Gate: run `python tools/check_public_api.py --path ../packages/sdypy-FRF` from the core repo and confirm exit 0 (including advisory drift report for any uncurated pyFRF names); run `python -m build` from `packages/sdypy-FRF`; fresh-venv install from a neutral directory path and confirm `python -c "import sdypy.FRF; print(sdypy.FRF.__all__)"` prints the expected list.

## 6. sdypy-excitation

- [x] 6.1 Rewrite `sdypy/excitation/__init__.py` in the `packages/sdypy-excitation` clone: replace `from pyExSi import *` with explicit imports of each of the eleven curated names: `from pyExSi import burst_random, get_kurtosis, get_psd, impulse, nonstationary_signal, normal_random, pseudo_random, random_gaussian, sine_sweep, stationary_nongaussian_signal, uniform_random`. Add `__all__ = ["burst_random", "get_kurtosis", "get_psd", "impulse", "nonstationary_signal", "normal_random", "pseudo_random", "random_gaussian", "sine_sweep", "stationary_nongaussian_signal", "uniform_random"]`. Confirm no `import *` line remains and `np`, `CubicSpline`, `beta`, `moment`, `signal`, `signals` are no longer accessible via `from sdypy.excitation import *`.
- [x] 6.2 Bump version to `0.2.0` in `pyproject.toml` (or fold into unreleased `0.1.2` if not yet on PyPI, superseding with `0.2.0`).
- [x] 6.3 Gate: run `python tools/check_public_api.py --path ../packages/sdypy-excitation` from the core repo and confirm exit 0 (including advisory drift report for any uncurated pyExSi names); run `python -m build` from `packages/sdypy-excitation`; fresh-venv install from a neutral directory path and confirm `python -c "import sdypy.excitation; print(sdypy.excitation.__all__)"` prints the expected list.

## 7. sdypy-view

- [x] 7.1 Rewrite `sdypy/view/__init__.py` in the `packages/sdypy-view` clone: add explicit imports for the five curated names (`Plotter3D`, `create_fem_mesh`, `prepare_animation_displacements`, `prepare_animation_field`, `copy_image_to_clipboard`). Add `__all__ = ["Plotter3D", "create_fem_mesh", "prepare_animation_displacements", "prepare_animation_field", "copy_image_to_clipboard"]`. Remove any star-imports or incidental re-exports (`np`, `pv`, `io`, `platform`, `subprocess`, `pyperclip`, `Image`, `BackgroundPlotter`, `BasePlotter`, `haspyqt`, `warnings`) from the exported surface.
- [x] 7.2 Bump version to `0.2.0` in `pyproject.toml` (or fold into unreleased `0.1.7` if not yet on PyPI, superseding with `0.2.0`).
- [x] 7.3 Gate: run `python tools/check_public_api.py --path ../packages/sdypy-view` from the core repo and confirm exit 0; run `python -m build` from `packages/sdypy-view`; fresh-venv install from a neutral directory path and confirm `python -c "import sdypy.view; print(sdypy.view.__all__)"` prints the expected list.

## 8. sdypy-model

- [x] 8.1 Rewrite `sdypy/model/__init__.py` in the `packages/sdypy-model` clone: add explicit imports for the six curated names (`Beam`, `Shell`, `Tetrahedron`, `solve_eigenvalue`, `lumped`, `mesh`). Add `__all__ = ["Beam", "Shell", "Tetrahedron", "solve_eigenvalue", "lumped", "mesh"]`. Confirm incidental submodule attributes (`beam`, `shell`, `tetrahedron`, `eigenvalue_solution`) are no longer listed in `__all__` (they may remain importable as module attributes, but are not part of the documented API).
- [x] 8.2 Add dual-accept parameter shims for `Tetrahedron`: canonical `young_modulus=`/`density=`/`poisson_ratio=`; the deprecated CapWords kwargs `Young=`/`Density=`/`Poisson=` MUST emit `DeprecationWarning` naming each deprecated parameter and forward to the canonical implementation. Positional callers unaffected.
- [x] 8.3 Add dual-accept parameter shim for `Beam`: canonical `young_modulus=`; deprecated `Young=` MUST emit `DeprecationWarning` and forward (`density` is already canonical). Positional callers unaffected.
- [x] 8.4 Add dual-accept parameter shims for `Shell`: canonical `young_modulus=`/`poisson_ratio=`/`density=`; the deprecated symbol kwargs `E=`/`nu=`/`rho=` MUST emit `DeprecationWarning` naming each deprecated parameter and forward to the canonical implementation. Positional callers unaffected.
- [x] 8.5 Add unit tests in `packages/sdypy-model/tests/` covering: (a) `Tetrahedron(young_modulus=..., density=..., poisson_ratio=...)` constructs without warning; (b) `Tetrahedron(Young=..., Density=..., Poisson=...)` constructs correctly and emits `DeprecationWarning`; (c) `Beam(young_modulus=...)` constructs without warning and `Beam(Young=...)` warns; (d) `Shell(young_modulus=..., poisson_ratio=..., density=...)` constructs without warning and `Shell(E=..., nu=..., rho=...)` warns with identical result; (e) every entry in `sdypy.model.__all__` resolves via `getattr`; (f) `lumped` and `mesh` entries are module objects; (g) `beam`, `tetrahedron`, `eigenvalue_solution` are NOT in `__all__`.
- [x] 8.6 Bump version to `0.2.0` in `pyproject.toml` (or fold into unreleased `0.1.5` if not yet on PyPI, superseding with `0.2.0`).
- [x] 8.7 Gate: run `python tools/check_public_api.py --path ../packages/sdypy-model` from the core repo and confirm exit 0; run `python -m build` from `packages/sdypy-model`; fresh-venv install from a neutral directory path and confirm `python -c "import sdypy.model; print(sdypy.model.__all__)"` prints the expected list.

## 9. Verification

- [x] 9.1 Run the full core pytest suite (`pytest tests/`) from the `sdypy` core repo. New `tests/test_public_api.py` tests MUST pass against the locally installed sibling clones. Record any tests that remain red against current PyPI artifacts as expected; those flip green per batched publish.
- [x] 9.2 Run `python tools/check_public_api.py --path ../packages/sdypy-<name>` for all six siblings and confirm all six exit 0 with no violations.
- [x] 9.3 Run `openspec validate standardize-public-api --strict` from the core repo root and confirm it passes with no errors.
- [x] 9.4 Cross-check: verify that the `namespace-packaging` spec tests still pass (no `sdypy/__init__.py` in any sibling wheel, lazy facade intact) to confirm this change did not inadvertently break the namespace setup from change #1.

## 10. Gated and team tasks

- [ ] 10.1 Team review of the curated `__all__` lists and the project-lead decisions of 2026-06-12: Q1 view helpers kept public; Q2 FEM material parameters unified on `young_modulus`/`poisson_ratio`/`density` across all three element classes; Q3 SEP 2 table extended now with `frf_form` + the material parameters (EMA `frf_type=` renamed with shim); Q4 umbrella `__all__` added with eager star-import behavior. Gate: obtain team sign-off before the batched PyPI releases.
- [ ] 10.2 After team sign-off: flip `docs/seps/sep-0002.rst` `:Status: Draft` to `Accepted` and add a `:Resolution:` line. This task may remain open at archive time if team review has not concluded.
- [ ] 10.3 Note for maintainer (outside these tasks): tag each sibling repo to trigger its release workflow and publish the minor releases to PyPI. Publish order: EMA 0.30.0, io 0.4.0, FRF 0.2.0, excitation 0.2.0, view 0.2.0, model 0.2.0. Once each package publishes, its distribution-layer tests in `tests/test_public_api.py` flip from expected-red to passing.
